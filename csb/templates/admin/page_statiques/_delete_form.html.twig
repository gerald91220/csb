<form method="post" action="{{ path('page_statiques_delete', {'id': page_statique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page_statique.id) }}">
    <button class="btn">Delete</button>
</form>
