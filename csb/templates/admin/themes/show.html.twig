{% extends 'admin/layout.html.twig' %}
{% block title %}Themes{% endblock %}

{% block content %}
    <h1>Themes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ theme.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ theme.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('themes_index') }}">back to list</a>

    <a href="{{ path('themes_edit', {'id': theme.id}) }}">edit</a>

    {{ include('admin/themes/_delete_form.html.twig') }}
{% endblock %}
