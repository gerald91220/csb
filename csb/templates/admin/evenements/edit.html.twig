{% extends 'admin/layout.html.twig' %}
{% block title %}Edit Themes{% endblock %}

{% block content %}
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <strong>Modifier</strong> Evenements
            </div>
            <div class="card-body card-block">
                {{ include('admin/evenements/_form.html.twig', {'button_label': 'Update'}) }}
                <div class="card-footer">  
                    <div class='inline-block'>
                    <button type="button" class="btn btn-secondary btn-sm " >
                        <a style="color: #fff;" href="{{ path('evenements_index') }}">
                            <i class="fa fa-list-ul"></i> Liste</a>
                    </button>   
                       {{ include('admin/evenements/_delete_form.html.twig') }}   
                    </div>
                </div>
            </div>             
        </div>  
    </div>
{% endblock %}
