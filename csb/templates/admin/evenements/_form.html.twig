{{ form_start(form) }}
    {{ form_widget(form) }}
        <div class="card-footer">
        <button type="submit" class="btn btn-primary btn-sm">
            <i class="fa fa-dot-circle-o"></i> {{ button_label|default('Sauvegader')|trans }}
        </button>
        </div>
{{ form_end(form) }}
